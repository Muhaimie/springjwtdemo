spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/jwttoken
    username: amigoscode
    password: password
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
